'use strict';

var omit = require('@chakra-ui/utils/omit');

function omitThemingProps(props) {
  return omit.omit(props, ["styleConfig", "size", "variant", "colorScheme"]);
}

exports.omitThemingProps = omitThemingProps;
