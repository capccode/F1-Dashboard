'use client';
'use strict';

var is = require('@chakra-ui/utils/is');

function isTag(target) {
  return is.isString(target) && (process.env.NODE_ENV !== "production" ? target.charAt(0) === target.charAt(0).toLowerCase() : true);
}
function getDisplayName(primitive) {
  return isTag(primitive) ? `chakra.${primitive}` : getComponentName(primitive);
}
function getComponentName(primitive) {
  return (process.env.NODE_ENV !== "production" ? is.isString(primitive) && primitive : false) || !is.isString(primitive) && primitive.displayName || !is.isString(primitive) && primitive.name || "ChakraComponent";
}

exports.getDisplayName = getDisplayName;
exports.isTag = isTag;
