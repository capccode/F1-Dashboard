'use client';
'use strict';

var jsxRuntime = require('react/jsx-runtime');
var runIfFn = require('@chakra-ui/utils/run-if-fn');
var stepContext = require('./step-context.cjs');

function StepStatus(props) {
  const { complete, incomplete, active } = props;
  const context = stepContext.useStepContext();
  let render = null;
  switch (context.status) {
    case "complete":
      render = runIfFn.runIfFn(complete, context);
      break;
    case "incomplete":
      render = runIfFn.runIfFn(incomplete, context);
      break;
    case "active":
      render = runIfFn.runIfFn(active, context);
      break;
  }
  return render ? /* @__PURE__ */ jsxRuntime.jsx(jsxRuntime.Fragment, { children: render }) : null;
}

exports.StepStatus = StepStatus;
