'use client';
'use strict';

var jsxRuntime = require('react/jsx-runtime');
var attr = require('@chakra-ui/utils/attr');
var cx = require('@chakra-ui/utils/cx');
var stepContext = require('./step-context.cjs');
var forwardRef = require('../system/forward-ref.cjs');
var factory = require('../system/factory.cjs');

const Step = forwardRef.forwardRef(function Step2(props, ref) {
  const { orientation, status, showLastSeparator } = stepContext.useStepContext();
  const styles = stepContext.useStepperStyles();
  return /* @__PURE__ */ jsxRuntime.jsx(
    factory.chakra.div,
    {
      ref,
      "data-status": status,
      "data-orientation": orientation,
      "data-stretch": attr.dataAttr(showLastSeparator),
      __css: styles.step,
      ...props,
      className: cx.cx("chakra-step", props.className)
    }
  );
});

exports.Step = Step;
