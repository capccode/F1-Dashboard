'use client';
'use strict';

var context = require('@chakra-ui/utils/context');

const [AvatarStylesProvider, useAvatarStyles] = context.createContext({
  name: `AvatarStylesContext`,
  hookName: `useAvatarStyles`,
  providerName: "<Avatar/>"
});

exports.AvatarStylesProvider = AvatarStylesProvider;
exports.useAvatarStyles = useAvatarStyles;
